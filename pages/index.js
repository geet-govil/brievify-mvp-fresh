// pages/index.js
import React from 'react';
import App from '../App'; // Import the main App component from the root

// This is the default export for the root page (/).
// It renders the main App component which contains all the routing and logic.
function HomePage() {
  return <App />;
}

export default HomePage;